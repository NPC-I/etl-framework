# ========== INCLUDE: Package Metadata ==========
# These files MUST be included in the PyPI package
include LICENSE
include README.md
include pyproject.toml
include setup.py

# ========== INCLUDE: Source Code ==========
# All Python source files in the package
recursive-include src/etl_framework *.py

# ========== EXCLUDE: Development Files ==========
# These should NOT be in the PyPI package

# Build and cache files
exclude *.pyc
exclude *.pyo
exclude *~
exclude .coverage

# ========== EXCLUDE: User Data & Config ==========
# These are application-specific, not package files
exclude data/
exclude demo/
exclude docs/
exclude examples/
exclude logs/
exclude mappings/
exclude scripts/
exclude tests/
exclude *.db
exclude *.sqlite

# ========== EXCLUDE: Git & IDE Files ==========
# Version control and IDE files
exclude .git
exclude .gitignore
exclude .vscode/
exclude .idea/

# ========== EXCLUDE: Environments ==========
# Virtual environments
exclude .venv
exclude venv/
exclude .env
exclude .env.example
exclude pytest.ini

# ========== EXCLUDE: Legacy Files ==========
# Old files that no longer exist (kept for safety)
exclude .continue/
